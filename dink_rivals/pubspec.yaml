name: dink_rivals
description: Phase 0 gray-box rally prototype for Dink Rivals.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flame: any
  go_router: ^17.2.3
  flutter_riverpod: ^3.3.1
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
